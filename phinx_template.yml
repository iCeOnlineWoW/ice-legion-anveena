paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: deployment

    deployment:
        adapter: mysql
        host: ${DBHOST}
        port: ${DBPORT}
        name: ${DBNAME}
        user: ${DBUSER}
        pass: ${DBPASS}
